/*!
 * \file main.cpp
 * \brief Точка входа серверного приложения.
 *
 * Инициализирует базу данных и запускает TCP-сервер.
 */
//! \brief Файл, содержащий главную функцию main - точку входа в серверное приложение

#include <QCoreApplication>
//! \brief Подключение класса консольного приложения Qt (без GUI)

#include "mytcpserver.h"
//! \brief Подключение заголовочного файла TCP-сервера

#include "database.h"
//! \brief Подключение заголовочного файла класса для работы с базой данных

/*!
 * \brief main — точка входа.
 * \param argc Количество аргументов командной строки
 * \param argv Аргументы командной строки
 * \return Код завершения приложения
 */
int main(int argc, char *argv[]) {
    //! \brief Главная функция - точка входа в серверное приложение
    //! \param argc Количество аргументов командной строки
    //! \param argv Массив строк с аргументами командной строки
    //! \return Код возврата (0 при успешном завершении, иначе код ошибки)

    QCoreApplication a(argc, argv);
    //! \brief Создает объект консольного приложения Qt, который управляет событийным циклом
    //! \details QCoreApplication используется вместо QApplication, так как сервер не имеет GUI
    //! \param argc Количество аргументов командной строки
    //! \param argv Массив аргументов командной строки

    // Инициализируем базу данных (Singleton)
    DataBase::getInstance()->init();
    //! \brief Получает единственный экземпляр класса DataBase и инициализирует базу данных
    //! \details Создает подключение к SQLite базе данных и создает необходимые таблицы
    //!          (Users, Stats), если они не существуют

    // Запускаем TCP-сервер
    MyTcpServer server;
    //! \brief Создает объект TCP-сервера
    //! \details В конструкторе сервер автоматически запускается и начинает
    //!          прослушивать входящие подключения на порту 33333

    return a.exec();
    //! \brief Запускает главный событийный цикл Qt
    //! \details Функция блокирует выполнение, пока сервер не будет остановлен.
    //!          Возвращает код завершения приложения (обычно 0 при успехе)
    //! \return Код завершения приложения
}
